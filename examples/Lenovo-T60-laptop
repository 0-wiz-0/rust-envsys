sensor acpiacad0
        sub-sensor connected
                cur-value => Integer(1)
                description => String("connected")
                index => String("sensor0")
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Indicator")
        device properties:
                device-class => String("ac-adapter")
                refresh-timeout => Integer(30)
sensor acpibat0
        sub-sensor present
                cur-value => Integer(1)
                description => String("present")
                index => String("sensor0")
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Indicator")
        sub-sensor design voltage
                allow-rfact => Boolean(false)
                cur-value => Integer(10800000)
                description => String("design voltage")
                index => String("sensor1")
                monitoring-supported => Boolean(false)
                rfact => Integer(0)
                state => String("valid")
                type => String("Voltage DC")
        sub-sensor voltage
                allow-rfact => Boolean(false)
                cur-value => Integer(12399000)
                description => String("voltage")
                index => String("sensor2")
                monitoring-supported => Boolean(false)
                rfact => Integer(0)
                state => String("valid")
                type => String("Voltage DC")
        sub-sensor design cap
                cur-value => Integer(84240000)
                description => String("design cap")
                index => String("sensor3")
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Watt hour")
        sub-sensor last full cap
                cur-value => Integer(51960000)
                description => String("last full cap")
                index => String("sensor4")
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Watt hour")
        sub-sensor charge
                critical-capacity => Integer(200000)
                cur-value => Integer(51280000)
                description => String("charge")
                index => String("sensor5")
                max-value => Integer(51960000)
                monitoring-state-hw-range-limit => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Watt hour")
                want-percentage => Boolean(true)
                warning-capacity => Integer(2598000)
        sub-sensor charge rate
                cur-value => Integer(1605000)
                description => String("charge rate")
                index => String("sensor6")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(false)
                state => String("invalid")
                type => String("Watts")
        sub-sensor discharge rate
                cur-value => Integer(0)
                description => String("discharge rate")
                index => String("sensor7")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(false)
                state => String("invalid")
                type => String("Watts")
        sub-sensor charging
                cur-value => Integer(0)
                description => String("charging")
                index => String("sensor8")
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Battery charge")
        sub-sensor charge state
                battery-capacity => String("NORMAL")
                cur-value => Integer(1)
                description => String("charge state")
                index => String("sensor9")
                monitoring-state-state-changed => Boolean(true)
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Battery capacity")
        device properties:
                device-class => String("battery")
                refresh-timeout => Integer(30)
sensor acpitz0
        sub-sensor temperature
                critical-max => Integer(400150000)
                cur-value => Integer(341150000)
                description => String("temperature")
                index => String("sensor0")
                monitoring-state-hw-range-limit => Boolean(true)
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Temperature")
        device properties:
                device-class => String("other")
                refresh-timeout => Integer(30)
sensor acpitz1
        sub-sensor cpu0/cpu1 temperature
                critical-max => Integer(372150000)
                cur-value => Integer(343150000)
                description => String("cpu0/cpu1 temperature")
                index => String("sensor0")
                monitoring-state-hw-range-limit => Boolean(true)
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(false)
                state => String("valid")
                type => String("Temperature")
        device properties:
                device-class => String("other")
                refresh-timeout => Integer(30)
sensor coretemp0
        sub-sensor cpu0 temperature
                cur-value => Integer(343150000)
                description => String("cpu0 temperature")
                index => String("sensor0")
                monitoring-state-critical => Boolean(true)
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        device properties:
                device-class => String("other")
                refresh-timeout => Integer(30)
sensor coretemp1
        sub-sensor cpu1 temperature
                cur-value => Integer(343150000)
                description => String("cpu1 temperature")
                index => String("sensor0")
                monitoring-state-critical => Boolean(true)
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        device properties:
                device-class => String("other")
                refresh-timeout => Integer(30)
sensor thinkpad0
        sub-sensor temperature 0
                cur-value => Integer(340150000)
                description => String("temperature 0")
                index => String("sensor0")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 1
                cur-value => Integer(313150000)
                description => String("temperature 1")
                index => String("sensor1")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 2
                cur-value => Integer(315150000)
                description => String("temperature 2")
                index => String("sensor2")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 3
                cur-value => Integer(332150000)
                description => String("temperature 3")
                index => String("sensor3")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 4
                cur-value => Integer(323150000)
                description => String("temperature 4")
                index => String("sensor4")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 5
                cur-value => Integer(0)
                description => String("temperature 5")
                index => String("sensor5")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("invalid")
                type => String("Temperature")
        sub-sensor temperature 6
                cur-value => Integer(305150000)
                description => String("temperature 6")
                index => String("sensor6")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("valid")
                type => String("Temperature")
        sub-sensor temperature 7
                cur-value => Integer(0)
                description => String("temperature 7")
                index => String("sensor7")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                state => String("invalid")
                type => String("Temperature")
        sub-sensor fan speed 0
                cur-value => Integer(3212)
                description => String("fan speed 0")
                index => String("sensor8")
                monitoring-state-refresh-event => Boolean(true)
                monitoring-supported => Boolean(true)
                rpms => Integer(0)
                state => String("invalid")
                type => String("Fan")
        device properties:
                device-class => String("other")
                refresh-timeout => Integer(30)
